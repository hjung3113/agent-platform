# lifetimeworkflow

Status: ADAPT

Evidence:
- README: contract-first template, single runtime-neutral source emitted to OpenCode/Claude,
  contract/golden/emit-drift gates.
- `.memory/README.md`: constitution, derived, state, process planes.

Adopt:
- human-owned constitution vs derived cache separation
- cross-runtime single-source generation/drift detection
- executable contract/golden gate philosophy

Modify:
- avoid duplicated committed volatile state if external run protocol already owns operational state.
- root `CONTEXT.md` is chosen as canonical vocabulary instead of keeping a second glossary copy.
