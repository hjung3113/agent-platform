# agent-migration-pipeline

Status: ADAPT / DOMAIN REFERENCE

Evidence:
- README/docs: discovery -> behavior contract -> design -> implement -> adversarial review -> verify.
- `docs/03-evidence-and-verification.md`: evidence grades, characterization captures,
  composite judge, explicit expected sources, mutation self-test.

Adopt:
- analyst/discovery role
- evidence hierarchy and explicit uncertainty
- composite evidence semantics
- verifier self-test with known-wrong mutation

Keep domain-specific:
- WPF/MSSQL/DLL migration details and feature-card schemas.
