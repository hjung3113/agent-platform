# orcaskills

Status: ADAPT / OPTIONAL UI

Evidence:
- README: visual DAG editor, role/profile/model policy, approval/condition/parallel nodes,
  validated workflow preview/run, Orca remains runtime authority.
- Agent Workflow template: architect -> isolated implementer -> reviewer -> verifier -> release decision.

Adopt:
- visual editor as optional projection/editor
- role/profile/model configuration UX
- decision gate and release-captain concepts

Do not adopt as core:
- UI must not become state authority or precede protocol implementation.
