# ADRs (template only)

This folder is a placeholder. Architecture decision records for the platform-in-progress
are authored and maintained in `../../../dev-env/docs/adr/`. Nothing here is authoritative.

Use `TEMPLATE.md` as the starting shape for an ADR once one is ready to publish alongside
the shipped product.
