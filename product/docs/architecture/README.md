# Architecture (template only)

This folder is a placeholder. Current architecture documentation for the platform-in-progress
lives in `../../../dev-env/docs/architecture/`. Nothing here is authoritative.

Use `TEMPLATE.md` as the starting shape for an architecture doc once one is ready to publish
alongside the shipped product.
