# Specs (template only)

This folder is a placeholder. Normative product specs are authored and maintained in
`../../../dev-env/docs/specs/` while the platform is being built. Nothing here is authoritative.

Use `TEMPLATE.md` as the starting shape for a new spec once one is ready to publish
alongside the shipped product.
