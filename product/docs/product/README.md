# Product (template only)

This folder is a placeholder. Current vision/scope/principles for the platform-in-progress
live in `../../../dev-env/docs/product/`. Nothing here is authoritative.

Use `TEMPLATE.md` as the starting shape for a product doc once one is ready to publish
alongside the shipped product.
